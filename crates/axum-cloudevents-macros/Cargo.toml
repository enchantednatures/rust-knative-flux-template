[package]
name = "axum-cloudevents-macros"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Proc macros for axum-cloudevents tagged enum routing"

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1.0"
quote = "1.0"
syn = { version = "3.0", features = ["full", "parsing", "extra-traits"] }
