apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImageRepository
metadata:
  name: rust-service
  namespace: flux-system
spec:
  image: localhost:5001/test-org/test-repo
  interval: 1m
  # Optional: For private container registries
  # secretRef:
  #   name: ghcr-credentials
