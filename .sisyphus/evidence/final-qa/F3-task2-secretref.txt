### F3 Task2 Scenario: secretRef active and URL switched
### CMD: grep -A2 'secretRef:' deploy/flux/git-repository.yaml
  secretRef:
    name: github-deploy-key
### secretRef grep EXIT_CODE=0 (0=pass)
### CMD: grep 'url:' deploy/flux/git-repository.yaml
  url: ssh://git@github.com/{{ github_org }}/{{ github_repo }}.git
### url grep EXIT_CODE=0
### EXTRA: grep -c 'https://github.com/{{' (expect 0 matches, exit 1)
0
### https-count EXIT_CODE=1 (1=zero matches=pass)
### EXTRA: ssh URL match count (expect exactly 1)
1
### EXIT_CODE=0
