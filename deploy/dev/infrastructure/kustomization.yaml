apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - redis.yaml
  # kafka.yaml and minio.yaml are Liquid templates processed by cargo-generate
  # They will be available after template generation
  # - kafka.yaml
  # - minio.yaml
