apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - redis.yaml
  {% if event_source_kafka %}
  - kafka.yaml
  {% endif %}
  {% if feature_s3 %}
  - minio.yaml
  {% endif %}
